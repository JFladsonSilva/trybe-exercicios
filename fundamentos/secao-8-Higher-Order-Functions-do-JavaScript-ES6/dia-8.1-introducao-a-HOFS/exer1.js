const emailListInData = [
    'roberta@email.com',
    'paulo@email.com',
    'anaroberta@email.com',
    'fabiano@email.com',
  ];
  
  // Adicione seu código aqui
  emailListInData.forEach((email) => {
    console.log(`O email ${email} está cadastrado em nosso banco de dados!`);
  })

  //Anotações
  //some = algum. Se algum elemento do array satisfaz uma condição, retorna true. Se não satisfaz retorna false.
  //every = se todos os elementos de um array satisfazem uma condição, retorna true. se não satisfaz retorna false